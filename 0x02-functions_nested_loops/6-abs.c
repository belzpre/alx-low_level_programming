#include "main.h"
/**
 * _abs - See the description
 * @r: input number
 * Description: This gives the absolute value of a number.
 * Return: absolute value of a number.
 */
int _abs(int r)
{
	if (r >= 0)
	{
		return (r);
	}
	else
		return (r * -1);
}

#include "main.h"
/**
 * print_alphabet - Check description
 * Description: This program/function prints the alphabet, in lowercase, followed by a new line.
 * Return: N/A
 */
void print_alphabet(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
		_putchar(i);
	_putchar('\n');
}
